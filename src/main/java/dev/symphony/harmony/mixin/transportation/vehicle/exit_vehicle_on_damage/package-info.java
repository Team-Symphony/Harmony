/**
 * FEATURE: Mobs exit the vehicle they're riding when damaged.
 * (by default only the ones mobs can auto-mount on on vanilla, configurable via datapack)
 * @author Trigam
 * @author Flatkat
 * @author axialeaa
 */

package dev.symphony.harmony.mixin.transportation.vehicle.exit_vehicle_on_damage;